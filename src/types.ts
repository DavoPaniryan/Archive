export interface IUser {
    id: number | string,
    name: string,
    surname: string,
     age: number,
     salary: number
}

export interface IInputUser {
    name: string,
    surname: string,
     age: number,
     salary: number
}